import { FilterAlphPipe } from './filter-alph.pipe';

describe('FilterAlphPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAlphPipe();
    expect(pipe).toBeTruthy();
  });
});
